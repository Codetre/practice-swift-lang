/* stored prop should have the default value when finished to initialize 
 * except Optional.
 */
struct Citizen {
    var name: String
    var birthdate: String
}

// Stored props(name, birthdate) has not given any value when init.
var citizen = Citizen()

